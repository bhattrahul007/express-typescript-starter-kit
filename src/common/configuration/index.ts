import DataSourceConfiguration from './datasource-configuration';
import ServerConfiguration from './server-configuration';
import { _webAppConfig, WebAppConfiguration } from './app-configuration';

import type {
  IController,
  IDataSourceConfiguration,
  IDataSourceOptions,
  IDatabaseTypes,
  IServerConfiguration,
  IServerConfigurationOptions,
  IWebAppConfiguration,
  IWebAppConfigurationOptions,
} from './types';

export type {
  IController,
  IDataSourceConfiguration,
  IDataSourceOptions,
  IDatabaseTypes,
  IServerConfiguration,
  IServerConfigurationOptions,
  IWebAppConfiguration,
  IWebAppConfigurationOptions,
};

export { DataSourceConfiguration, ServerConfiguration, _webAppConfig, WebAppConfiguration };
